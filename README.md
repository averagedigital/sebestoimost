# Калькулятор Себестоимости

Расчет себестоимости упаковки BOPP/CPP по формулам ТЗ.

## Запуск
```bash
docker-compose up -d --build
```

## API
- `POST /api/calculate` — расчет
- `GET /ui` — web-интерфейс

